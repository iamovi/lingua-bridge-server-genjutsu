# Allowed frontend origin for CORS.
# Use * to allow all origins, or set a specific URL.
# Set this in Cloudflare Dashboard → Workers → Settings → Variables
# (Do NOT put secrets in wrangler.toml for production)
FRONTEND_ORIGIN=http://localhost:8080
